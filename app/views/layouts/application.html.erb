<!DOCTYPE html>
<html>
  <head>
    <title>Drive_spot</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <% if end_user_signed_in? %>
    <header>
      <div><%= link_to "トップ", root_path %></div>
      <div><%#= link_to "投稿する", new_post_path, class: "text-white" %></div>
      <div><%#= link_to "投稿一覧", posts_path, class: "text-white" %></div>
      <div><%= link_to "ログアウト", destroy_end_user_session_path, method: :delete %></div>
   </header>
  <% end %>

  <body>
    <%= yield %>
  </body>
</html>
